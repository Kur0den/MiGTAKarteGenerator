version: '3'
services:
  web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./.config/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./flontend:/usr/share/nginx/html
    # depends_on:
    #   - app
